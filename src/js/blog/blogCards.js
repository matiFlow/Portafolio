class Blog {
    constructor(blog) {
        this.img = "https://localhost:7043" + blog.urlImage;
        this.title = blog.title;
        this.description = blog.description;
        this.enlace = blog.enlace
    }
}